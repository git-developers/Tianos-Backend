{% spaceless %}

    {% for entity in objectsFour %}

        {% set id = entity.id|default('< missing ID >') %}
        {% set name = entity.name_box|default('< missing name_box >') %}

        <li
            class="{{ boxFour.li_class }}"
            data-id="{{ id }}"
            data-toggle="tooltip"
            data-original-title="{{ name }}"
        >

            <span class="handle ui-sortable-handle">
                <i class="fa fa-ellipsis-v"></i>
                <i class="fa fa-ellipsis-v"></i>
            </span>

            <input type="checkbox"
                   name="{{ boxFour.li_input_name }}"
                   value="{{ id }}">
            &nbsp;
            <span class="badge {{ boxFour.li_badge_class }}">
                {{ id }}
            </span>

            <span class="text box-truncate">
                {{ name }}
            </span>

            {#<div class="tools">#}
            {#<i class="fa fa-edit"></i>#}
            {#<i class="fa fa-trash-o"></i>#}
            {#</div>#}
        </li>
    {% else %}
        <li>
            <span class="text">
                No hay resultados.
            </span>
        </li>
    {% endfor %}

{% endspaceless %}
